<Window x:Class="PharmaBilling.Source.Views.AccountingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Accounting &amp; Ledgers" Height="600" Width="900" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Color="#000000" Opacity="0.05" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
    </Window.Resources>

    <Grid Background="#F4F7F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="ACCOUNTING &amp; LEDGERS" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Refresh" Width="100" Height="35" Background="#3498DB" Foreground="White" BorderThickness="0" Click="Refresh_Click" Cursor="Hand" Margin="0,0,10,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Statistics Section -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" Margin="10">
                
                <!-- This Week Card -->
                <Border Style="{StaticResource StatCard}" Width="250">
                    <StackPanel>
                        <TextBlock Text="THIS WEEK (7 DAYS)" FontWeight="Bold" Foreground="#3498DB" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="Total Sales" Style="{StaticResource StatLabel}"/>
                                <TextBlock Text="{Binding WeekSales}" Style="{StaticResource StatValue}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                                <TextBlock Text="Total Purchases" Style="{StaticResource StatLabel}"/>
                                <TextBlock Text="{Binding WeekPurchases}" Style="{StaticResource StatValue}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2">
                                <TextBlock Text="Net Profit" Style="{StaticResource StatLabel}" Foreground="#27AE60" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding WeekProfit}" Style="{StaticResource StatValue}" Foreground="#27AE60" FontSize="18"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- This Month Card -->
                <Border Style="{StaticResource StatCard}" Width="250">
                    <StackPanel>
                        <TextBlock Text="THIS MONTH (30 DAYS)" FontWeight="Bold" Foreground="#9B59B6" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="Total Sales" Style="{StaticResource StatLabel}"/>
                                <TextBlock Text="{Binding MonthSales}" Style="{StaticResource StatValue}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                                <TextBlock Text="Total Purchases" Style="{StaticResource StatLabel}"/>
                                <TextBlock Text="{Binding MonthPurchases}" Style="{StaticResource StatValue}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2">
                                <TextBlock Text="Net Profit" Style="{StaticResource StatLabel}" Foreground="#27AE60" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding MonthProfit}" Style="{StaticResource StatValue}" Foreground="#27AE60" FontSize="18"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 12 Month Card -->
                <Border Style="{StaticResource StatCard}" Width="250">
                    <StackPanel>
                        <TextBlock Text="LAST 12 MONTHS" FontWeight="Bold" Foreground="#E67E22" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="Total Sales" Style="{StaticResource StatLabel}"/>
                                <TextBlock Text="{Binding YearSales}" Style="{StaticResource StatValue}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                                <TextBlock Text="Total Purchases" Style="{StaticResource StatLabel}"/>
                                <TextBlock Text="{Binding YearPurchases}" Style="{StaticResource StatValue}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2">
                                <TextBlock Text="Net Profit" Style="{StaticResource StatLabel}" Foreground="#27AE60" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding YearProfit}" Style="{StaticResource StatValue}" Foreground="#27AE60" FontSize="18"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="2" Margin="20,0,20,20" Background="White" CornerRadius="8" Padding="10">
            <DataGrid x:Name="dgLedgers" ItemsSource="{Binding Ledgers}" SelectedItem="{Binding SelectedLedger}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ledger Name" Width="2*" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Type" Width="*" Binding="{Binding Type}"/>
                    <DataGridTextColumn Header="Current Balance" Width="150" Binding="{Binding Balance, StringFormat='Rs. {0:N2}'}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
