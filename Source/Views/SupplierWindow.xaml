<Window x:Class="PharmaBilling.Source.Views.SupplierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supplier Management" Height="500" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Background="#F4F7F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <TextBlock Text="SUPPLIER MANAGEMENT" FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>
        </Border>

        <Border Grid.Row="1" Margin="20,20,20,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="+ Add Supplier" Background="#2ECC71" Foreground="White" Padding="15,8" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="AddSupplier_Click"/>
                <Button Content="Edit Supplier" Background="#F39C12" Foreground="White" Padding="15,8" FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand" Click="EditSupplier_Click"/>
                <Button Content="Delete Supplier" Background="#E74C3C" Foreground="White" Padding="15,8" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="DeleteSupplier_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Margin="20" Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Contact" Width="120" Binding="{Binding Contact}"/>
                    <DataGridTextColumn Header="Email" Width="150" Binding="{Binding Email}"/>
                    <DataGridTextColumn Header="Address" Width="200" Binding="{Binding Address}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
