<UserControl x:Class="PharmaBilling.Source.Views.AnalysisUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Background="#F4F7F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Header & Filters -->
        <Border Grid.Row="0" Background="White" Padding="20" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Graphical Analysis" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" />
                    <TextBlock Text="Top Selling Medicines Volume" FontSize="14" Foreground="#7F8C8D" Margin="0,5,0,0" />
                </StackPanel>

                <!-- Filter Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Base button style local to this panel -->
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Padding" Value="15,8"/>
                            <Setter Property="Margin" Value="0,0,10,0"/>
                            <Setter Property="Background" Value="#ECF0F1"/>
                            <Setter Property="Foreground" Value="#2C3E50"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" CornerRadius="18" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#BDC3C7"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </StackPanel.Resources>
                    
                    <Button x:Name="btnToday" Content="Today" Click="Today_Click" />
                    <Button x:Name="btnWeekly" Content="Weekly" Click="Weekly_Click" />
                    <Button x:Name="btnMonthly" Content="Monthly" Click="Monthly_Click" Margin="0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Chart Area -->
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="20">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding TopMedicines}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/> <!-- Name -->
                                    <ColumnDefinition Width="*"/>   <!-- Bar Chart -->
                                    <ColumnDefinition Width="100"/> <!-- values -->
                                </Grid.ColumnDefinitions>
                                
                                <!-- Tooltip includes Revenue -->
                                <Grid.ToolTip>
                                    <ToolTip Background="#2C3E50" Foreground="White" BorderThickness="0" Placement="Right">
                                        <StackPanel Margin="5">
                                            <TextBlock Text="{Binding MedicineName}" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding TotalSold, StringFormat=Total Sold: {0}}" />
                                            <TextBlock Text="{Binding Revenue, StringFormat=Revenue: {0}}" />
                                        </StackPanel>
                                    </ToolTip>
                                </Grid.ToolTip>

                                <!-- Label -->
                                <TextBlock Grid.Column="0" Text="{Binding MedicineName}" Foreground="#34495E" FontSize="14" FontWeight="Medium" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,10,0"/>

                                <!-- Native Bar implementation using Grid proportional Columns via custom converter-less trick:
                                     We bind the column Width directly since GridLengthConverter supports star strings like "80*" -->
                                <Grid Grid.Column="1" VerticalAlignment="Center" Height="25">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="{Binding BarWidth}"/>
                                        <ColumnDefinition Width="{Binding RemainingWidth}"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border Grid.Column="0" Background="{Binding Color}" CornerRadius="0,5,5,0" />
                                </Grid>

                                <!-- Value -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding TotalSold}" FontWeight="Bold" FontSize="15" Foreground="#2C3E50" />
                                    <TextBlock Text=" units" Foreground="#95A5A6" FontSize="12" VerticalAlignment="Bottom" Margin="2,0,0,1"/>
                                </StackPanel>
                                
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

    </Grid>
</UserControl>
