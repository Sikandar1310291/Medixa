<UserControl x:Class="PharmaBilling.Source.Views.CustomerUC"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           >
    <Grid Background="#F4F7F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="CUSTOMER MANAGEMENT" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                <StackPanel x:Name="spAdminActions" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="+ Add Customer"   Background="#27AE60" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="AddCustomer_Click" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button Content="Edit"   Background="#3498DB" Foreground="White" BorderThickness="0" Click="EditCustomer_Click" Cursor="Hand" Margin="0,0,10,0"/>
                    <Button Content="Delete"   Background="#E74C3C" Foreground="White" BorderThickness="0" Click="DeleteCustomer_Click" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="8" Padding="10">
            <DataGrid EnableRowVirtualization="True" VirtualizingStackPanel.IsVirtualizing="True" VirtualizingStackPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" x:Name="dgCustomers" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Customer Name"  Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Contact No"  Binding="{Binding Contact}"/>
                    <DataGridTextColumn Header="Email"  Binding="{Binding Email}"/>
                    <DataGridTextColumn Header="Balance"  Binding="{Binding Balance, StringFormat='Rs. {0:N2}'}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
