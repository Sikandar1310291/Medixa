<Window x:Class="PharmaBilling.Source.Views.MedicineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Medixa - Medicine Management" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#F4F7F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Tool Bar -->
        <Border Grid.Row="0" Background="White" Padding="15" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Button Content="+ Add Medicine" Click="AddMedicine_Click" Padding="15,8" Background="#3498DB" Foreground="White" BorderThickness="0" Margin="0,0,10,0"/>
                    <Button Content="Import Excel" Click="ImportExcel_Click" Padding="15,8" Background="#27AE60" Foreground="White" BorderThickness="0" Margin="0,0,10,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBox Width="200" Height="35" VerticalContentAlignment="Center" Padding="5" Margin="0,0,5,0"/>
                    <Button Content="Search" Padding="15,8" Background="#BDC3C7" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Grid -->
        <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Medicines}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Med ID" Binding="{Binding MedicineID}" Width="60"/>
                    <DataGridTextColumn Header="Medicine Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn Header="TP" Binding="{Binding PurchasePrice}" Width="100"/>
                    <DataGridTextColumn Header="Retail" Binding="{Binding SalePrice}" Width="100"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding TotalStock}" Width="80"/>
                    <DataGridTemplateColumn Header="Action" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Click="EditMedicine_Click" Foreground="Blue" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,5,0"/>
                                    <Button Content="Delete" Click="DeleteMedicine_Click" Foreground="Red" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
