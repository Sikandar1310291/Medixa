<Window x:Class="PharmaBilling.Source.Views.PurchaseReturnWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Purchase Return" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner" Background="#F8F9FA" KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style x:Key="HeaderLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#C0392B" Padding="18,14">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="↩" FontSize="20" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel>
                    <TextBlock Text="PURCHASE RETURN" FontSize="16" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Return goods to supplier and adjust stock" FontSize="11" Foreground="#FFCDD2"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Controls -->
        <Border Grid.Row="1" Background="White" Margin="15,15,15,8" Padding="18" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="Select Purchase Invoice" Style="{StaticResource HeaderLabel}"/>
                    <ComboBox x:Name="cboPurchase" ItemsSource="{Binding AllPurchases}"
                              SelectedItem="{Binding SelectedPurchase}"
                              DisplayMemberPath="Display"
                              IsEditable="True" Padding="8" BorderBrush="#DDD" BorderThickness="1"
                              FontSize="12"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="Return Date" Style="{StaticResource HeaderLabel}"/>
                    <DatePicker x:Name="dpReturnDate" SelectedDate="{x:Static system:DateTime.Now}"
                                xmlns:system="clr-namespace:System;assembly=mscorlib"
                                Padding="8" BorderBrush="#DDD" BorderThickness="1"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Reason for Return" Style="{StaticResource HeaderLabel}"/>
                    <TextBox Text="{Binding Reason}" Padding="8" BorderBrush="#DDD" BorderThickness="1"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Items Grid -->
        <Border Grid.Row="2" Background="White" Margin="15,0,15,8" Padding="12" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="📦  Items from Selected Invoice — Set Return Qty for each item"
                           FontSize="12" Foreground="#555" Margin="0,0,0,8" FontStyle="Italic"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding ReturnItems}" AutoGenerateColumns="False"
                          CanUserAddRows="False" GridLinesVisibility="Horizontal"
                          Background="White" BorderBrush="#EEE" BorderThickness="1"
                          RowHeaderWidth="0" AlternatingRowBackground="#FAFAFA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Medicine" Binding="{Binding MedicineName}" IsReadOnly="True" Width="2.5*"/>
                        <DataGridTextColumn Header="Batch No" Binding="{Binding BatchNo}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Purchased Qty" Binding="{Binding MaxQty}" IsReadOnly="True" Width="*">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Foreground" Value="#2980B9"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Return Qty" Binding="{Binding ReturnQty, UpdateSourceTrigger=PropertyChanged}" Width="*">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Foreground" Value="#C0392B"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#E74C3C"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Purchase Price" Binding="{Binding PurchasePrice, StringFormat=N2}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Return Amount" Binding="{Binding TotalPrice, StringFormat=Rs. \{0:N2\}}" IsReadOnly="True" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#27AE60"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="White" Margin="15,0,15,15" Padding="18,14" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚠ " Foreground="#E67E22" FontSize="14"/>
                    <TextBlock Text="Only items with Return Qty > 0 will be processed. Stock will be deducted." 
                               Foreground="#7F8C8D" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Total Return: " FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat=Rs. \{0:N2\}}"
                               FontSize="16" FontWeight="Bold" Foreground="#C0392B"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Cancel" Width="90" Margin="0,0,10,0"
                        Background="#ECF0F1" Foreground="#2C3E50" Style="{StaticResource ActionBtn}"
                        Click="Cancel_Click" IsCancel="True"/>
                <Button Grid.Column="3" Content="✔ Process Return" Background="#C0392B" Foreground="White"
                        Style="{StaticResource ActionBtn}" Click="SaveReturn_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
